# iShell v1.0.0 开发路线图

## 📅 版本信息

- **版本**: v1.0.0
- **计划开始**: 2026-02-03
- **预计完成**: 2026-03-15 (6周)
- **基于版本**: v0.3.0
- **开发模式**: 增量开发，每个阶段独立测试

---

## 🎯 总体目标

将 iShell 打造成一个**企业级**的 SSH 管理工具，支持：
- 多会话并发管理
- 高级网络功能（端口转发、代理）
- 可扩展架构（插件系统）
- 专业级用户体验

---

## 📊 功能概览

| Phase | 功能 | 工作量 | 优先级 | 预计时间 |
|-------|------|--------|--------|----------|
| Phase 1 | 多窗口/标签页支持 | 高 | P0 | 2 周 |
| Phase 2 | SSH 端口转发 | 中 | P0 | 1 周 |
| Phase 3 | SSH 代理支持 | 中 | P1 | 1 周 |
| Phase 4 | 插件系统 | 高 | P1 | 2 周 |

**总计**: 6 周开发周期

---

## 🚀 Phase 1: 多窗口/标签页支持

### 目标
允许用户同时打开多个 SSH 会话，每个会话在独立的标签页中运行。

### 核心功能

#### 1.1 标签页管理器
```rust
// src/tabs/mod.rs
pub struct TabManager {
    tabs: Vec<Tab>,
    active_tab: usize,
    next_tab_id: usize,
}

pub struct Tab {
    id: usize,
    title: String,
    connection_id: Option<usize>,
    terminal_state: TerminalState,
    sftp_state: Option<SftpState>,
    created_at: SystemTime,
}
```

**功能清单**:
- [x] 标签页数据结构
- [ ] 创建新标签页
- [ ] 关闭标签页
- [ ] 切换标签页（快捷键: Ctrl+Tab, Ctrl+Shift+Tab）
- [ ] 标签页重命名
- [ ] 标签页拖放重排序

#### 1.2 UI 集成
- [ ] 顶部标签栏 UI
- [ ] 每个标签显示连接状态图标
- [ ] 右键菜单（关闭、复制、重命名）
- [ ] 标签页溢出处理（下拉菜单）
- [ ] 新建标签按钮 (+)

#### 1.3 状态隔离
每个标签页独立维护：
- [ ] SSH 会话状态
- [ ] 终端输出缓冲区
- [ ] 命令历史
- [ ] SFTP 文件浏览状态
- [ ] AI 对话历史

#### 1.4 快捷键
- `Ctrl+T`: 新建标签页
- `Ctrl+W`: 关闭当前标签页
- `Ctrl+Tab`: 切换到下一个标签页
- `Ctrl+Shift+Tab`: 切换到上一个标签页
- `Ctrl+1-9`: 快速切换到第 N 个标签页

### 技术挑战
1. **状态管理**: 每个标签页需要独立的 `AppState` 副本
2. **内存优化**: 避免不活跃标签占用过多内存
3. **线程安全**: 多个 SSH 会话并发运行

### 测试计划
- [ ] 创建/关闭标签页
- [ ] 多标签并发连接
- [ ] 快捷键功能
- [ ] 内存泄漏测试
- [ ] 标签切换性能测试

### 预计时间: **2 周**

---

## 🔌 Phase 2: SSH 端口转发

### 目标
支持本地端口转发、远程端口转发和动态端口转发（SOCKS 代理）。

### 核心功能

#### 2.1 本地端口转发 (Local Port Forwarding)
```rust
// src/port_forward/mod.rs
pub struct LocalForward {
    local_host: String,      // 通常是 "127.0.0.1"
    local_port: u16,         // 本地监听端口
    remote_host: String,     // 远程目标主机
    remote_port: u16,        // 远程目标端口
    status: ForwardStatus,
}

// 示例: ssh -L 8080:remote-db:3306 user@server
```

**使用场景**:
- 访问远程数据库（如 MySQL, PostgreSQL）
- 访问内网 Web 服务
- 绕过防火墙限制

#### 2.2 远程端口转发 (Remote Port Forwarding)
```rust
pub struct RemoteForward {
    remote_host: String,     // 远程监听地址
    remote_port: u16,        // 远程监听端口
    local_host: String,      // 本地目标主机
    local_port: u16,         // 本地目标端口
    status: ForwardStatus,
}

// 示例: ssh -R 9000:localhost:8000 user@server
```**:
- 共享本地开发服务器
- 远程访问本地应用
- 反向代理设置

#### 2.3 动态端口转发 (Dynamic Port Forwarding)
```rust
pub struct DynamicForward {
    local_host: String,      // 通常是 "127.0.0.1"
    local_port: u16,         // SOCKS 代理端口
    status: ForwardStatus,
}

// 示例: ssh -D 1080 user@server
// 创建 SOCKS5 代理，可配置浏览器使用
```

**使用场景**:
- 浏览器代理
- 全局流量转发
- 安全访问内网资源

#### 2.4 UI 设计
```
端口转发管理器窗口:
┌────────────────────────────────────────┐
│ 🔌 端口转发管理                        │
├────────────────────────────────────────┤
│ 连接: Production Server               │
│                                        │
│ ┌──────────────────────────────────┐  │
│ │ 活跃转发 (2)                      │  │
│ │ ✅ Local: 8080 → remote-db:3306   │  │
│ │ ✅ Dynamic: SOCKS5 on 1080        │  │
│ └──────────────────────────────────┘  │
│                                        │
│ [➕ 新建转发]                          │
│                                        │
│ 转发类型: [本地 ▼]                    │
│ 本地端口: [____]                       │
│ 远程主机: [________________]           │
│ 远程端口: [____]                       │
│                                        │
│ [创建] [取消]                          │
└────────────────────────────────────────┘
```

### 功能清单
- [ ] 本地端口转发实现
- [ ] 远程端口转发实现
- [ ] 动态端口转发（SOCKS5）实现
- [ ] 端口转发管理 UI
- [ ] 转发状态监控
- [ ] 自动重连机制
- [ ] 端口冲突检测
- [ ] 转发配置持久化

### 技术实现
```rust
impl SshSession {
    pub fn create_local_forward(
        &self,
        local_port: u16,
        remote_host: &str,
        remote_port: u16,
    ) -> Result<ForwardHandle> {
        // 使用 ssh2::Channel::direct_tcpip
        let channel = self.session.channel_direct_tcpip(
            remote_host,
            remote_port,
            None,
        )?;
        
        // 创建本地监听 socket
        let listener = TcpListener::bind(format!("127.0.0.1:{}", local_port))?;
        
        // 启动转发线程
        spawn_forward_thread(listener, channel);
        
        Ok(ForwardHandle { ... })
    }
    
    pub fn create_dynamic_forward(&self, local_port: u16) -> Result<ForwardHandle> {
        // 实现 SOCKS5 代理服务器
        // 使用 tokio-socks 或手动实现 SOCKS5 协议
    }
}
```

### 测试计划
- [ ] 本地转发连接测试
- [ ] 远程转发连接测试
- [ ] SOCKS5 代理功能测试
- [ ] 端口冲突处理测试
- [ ] 断线重连测试
- [ ] 性能测试（高并发）

### 预计时间: **1 周**

---

## 🔗 Phase 3: SSH 代理支持 (Jump Host)

### 目标
支持通过跳板机（堡垒机）连接到目标服务器。

### 核心功能

#### 3.1 跳板机配置
```rust
// src/types.rs
pub struct ProxyConfig {
    pub jump_host: String,
    pub jump_port: u16,
    pub jump_username: String,
    pub jump_auth: AuthMethod,
}

// 在 SshConfig 中添加
pub struct SshConfig {
    // ... 现有字段
    pub proxy: Option<ProxyConfig>,
}
```

#### 3.2 连接流程
```
用户 → 跳板机 → 目标服务器

1. 连接到跳板机
2. 通过跳板机建立到目标服务器的通道
3. 在通道上创建 SSH 会话
```

#### 3.3 实现方式
```rust
impl SshSession {
    pub fn connect_via_proxy(&self, proxy: &ProxyConfig) -> Result<()> {
        // 1. 连接到跳板机
        let proxy_session = Session::new()?;
        proxy_session.set_tcp_stream(TcpStream::connect(
            format!("{}:{}", proxy.jump_host, proxy.jump_port)
        )?);
        proxy_session.handshake()?;
        // 认证跳板机
        authenticate(&proxy_session, &proxy.jump_auth)?;
        
        // 2. 通过跳板机建立通道到目标服务器
        let channel = proxy_session.channel_direct_tcpip(
            &self.host,
            self.port,
            None,
        )?;
        
        // 3. 在通道上创建目标服务器的 SSH 会话
        let target_session = Session::new()?;
        target_session.set_tcp_stream(channel);
        target_session.handshake()?;
        // 认证目标服务器
        authenticate(&target_session, &self.auth)?;
        
        self.session = target_session;
        Ok(())
    }
}
```

#### 3.4 UI 设计
在 "New Connection" 对话框中添加：
```
┌─ 代理设置 ─────────────────────┐
│ □ 使用跳板机                    │
│                                │
│ 跳板机地址: [________________] │
│ 端口: [22]                     │
│ 用户名: [____________]         │
│ 认证方式: [密码 ▼]             │
│ 密码/密钥: [**********]        │
└────────────────────────────────┘
```

### 功能清单
- [ ] ProxyConfig 数据结构
- [ ] 通过跳板机连接实现
- [ ] UI 集成（新建连接对话框）
- [ ] 配置持久化
- [ ] 多级跳板支持（可选）
- [ ] 连接状态显示（显示跳板机路径）

### 测试计划
- [ ] 单级跳板机连接测试
- [ ] 多级跳板机测试（可选）
- [ ] 跳板机认证失败处理
- [ ] 跳板机断线重连

### 预计时间: **1 周**

---

## 🧩 Phase 4: 插件系统

### 目标
允许用户和第三方开发者扩展 iShell 功能，无需修改核心代码。

### 架构设计

#### 4.1 插件接口定义
```rust
// src/plugin/mod.rs
pub trait Plugin: Send + Sync {
    /// 插件元数据
    fn metadata(&self) -> PluginMetadata;
    
    /// 插件初始化
    fn on_load(&mut self, context: &PluginContext) -> Result<()>;
    
    /// 插件卸载
    fn on_unload(&mut self) -> Result<()>;
    
    /// 处理命令
    fn on_command(&mut self, command: &str, args: &[&str]) -> Option<String>;
    
    /// 处理 SSH 输出（可选）
    fn on_ssh_output(&mut self, output: &str) -> Option<String>;
    
    /// 渲染 UI（可选）
    fn render_ui(&mut self, ui: &mut egui::Ui, ctx: &PluginContext);
}

pub struct PluginMetadata {
    pub name: String,
    pub version: String,
    pub author: String,
    pub description: String,
    pub capabilities: Vec<PluginCapability>,
}

pub enum PluginCapability {
    CommandHandler,    // 处理自定义命令
    OutputFilter,      // 过滤/修改 SSH 输出
    UiPanel,          // 添加 UI 面板
    KeyBinding,       // 注册快捷键
}
```

#### 4.2 插件加载器
```rust
pub struct PluginManager {
    plugins: HashMap<String, Box<dyn Plugin>>,
    plugin_dir: PathBuf,
}

impl PluginManager {
    pub fn load_plugins(&mut self) -> Result<()> {
        // 扫描插件目录
        // 支持两种加载方式:
        // 1. 动态库 (.dylib/.so/.dll)
        // 2. WebAssembly (.wasm) - 更安全
    }
    
    pub fn execute_command(&mut self, command: &str) -> Option<String> {
        // 尝试每个插件处理命令
        for plugin in self.plugins.values_mut() {
            if let Some(result) = plugin.on_command(command, &[]) {
                return Some(result);
            }
        }
        None
    }
}
```

#### 4.3 示例插件: JSON 美化器
```rust
// plugins/json_formatter.rs
pub struct JsonFormatterPlugin;

impl Plugin for JsonFormatterPlugin {
    fn metadata(&self) -> PluginMetadata {
        PluginMetadata {
            name: "JSON Formatter".to_string(),
            version: "1.0.0".to_string(),
            author: "iShell Team".to_string(),
            description: "Automatically format JSON output".to_string(),
            capabilities: vec![PluginCapability::OutputFilter],
        }
    }
    
    fn on_ssh_output(&mut self, output: &str) -> Option<String> {
        // 检测是否是 JSON
        if let Ok(json) = serde_json::from_str::<serde_json::Value>(output) {
            // 美化 JSON
            Some(serde_json::to_string_pretty(&json).unwrap())
        } else {
            None
        }
    }
}
```

#### 4.4 插件安全性
- **沙箱执行**: 使用 WebAssembly 隔离插件
- **权限系统**: 插件需要声明所需权限
- **代码签名**: 验证插件来源
- **审计日志**: 记录插件行为

#### 4.5 插件市场 UI
```
┌────────────────────────────────────────┐
│ 🧩 插件市场                            │
├────────────────────────────────────────┤
│ 搜索: [____________] [🔍]             │
│                                        │
│ ┌──────────────────────────────────┐  │
│ │ 已安装 (3)                        │  │
│ │ ✅ JSON Formatter v1.0.0          │  │
│ │ ✅ Syntax Highlighter v2.1.0      │  │
│ │ ✅ Auto Complete v1.5.0           │  │
│ └──────────────────────────────────┘  │
│                                        │
│ ┌──────────────────────────────────┐  │
│ │ 可用插件 (15)                     │  │
│ │ 📦 Git Helper v1.0.0              │  │
│ │    自动补全 git 命令               │  │
│ │    [安装]                          │  │
│ │                                   │  │
│ │ 📦 Log Analyzer v2.0.0            │  │
│ │    分析和高亮日志文件              │  │
│ │    [安装]                          │  │
│ └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 功能清单
- [ ] Plugin trait 定义
- [ ] PluginManager 实现
- [ ] 动态库加载（使用 libloading）
- [ ] WebAssembly 支持（使用 wasmer/wasmtime）
- [ ] 插件配置系统
- [ ] 插件市场 UI
- [ ] 3-5 个示例插件
- [ ] 插件开发文档

### 示例插件

#### 1. JSON Formatter
- 自动检测和美化 JSON 输出

#### 2. Syntax Highlighter
- 为常见文件类型提供语法高亮（YAML, XML, JSON）

#### 3. Auto Complete
- 基于历史命令的智能补全

#### 4. Git Helper
- Git 命令简化和可视化

#### 5. Log Analyzer
- 实时分析日志文件，高亮错误和警告

### 插件开发工具链
```bash
# 创建新插件模板
ishell-plugin new my-plugin

# 构建插件
cd my-plugin
cargo build --release

# 测试插件
ishell-plugin test

# 发布插件
ishell-plugin publish
```

### 测试计划
- [ ] 插件加载/卸载
- [ ] 插件命令处理
- [ ] 插件 UI 渲染
- [ ] 插件隔离性测试
- [ ] 性能测试（多插件并发）
- [ ] 安全性测试

### 预计时间: **2 周**

---

## 🔧 技术栈更新

### 新增依赖

```toml
[dependencies]
# 插件系统
libloading = "0.8"          # 动态库加载
wasmer = "4.2"              # WebAssembly 运行时

# 端口转发
tokio-socks = "0.5"         # SOCKS5 代理

# UI 增强
egui_dock = "0.11"          # 标签页/停靠面板
egui_extras = "0.27"        # 额外 UI 组件
```

---

## 📈 性能目标

| 指标 | v0.3.0 | v1.0.0 目标 |
|------|--------|-------------|
| 启动时间 | ~2s | <1.5s |
| 内存占用 | ~50MB | <80MB (10 标签) |
| 二进制大小 | ~3MB | <5MB |
| 并发会话数 | 5 | 50+ |
| 端口转发延迟 | N/A | <5ms |

---

## 🧪 测试策略

### 单元测试
- 每个新模块至少 80% 代码覆盖率
- 标签管理器测试
- 端口转发测试
- 插件系统测试

### 集成测试
- 多标签并发操作
- 跨标签状态隔离
- 插件与核心功能交互

### 性能测试
- 50+ 并发连接压力测试
- 内存泄漏检测（valgrind/heaptrack）
- 端口转发吞吐量测试

### 安全测试
- 插件沙箱逃逸测试
- 端口转发权限检查
- 配置文件注入测试

---

## 📝 文档计划

### 用户文档
- [ ] 标签页使用指南
- [ ] 端口转发配置教程
- [ ] 跳板机设置指南
- [ ] 插件安装和使用

### 开发者文档
- [ ] 插件开发指南
- [ ] API 参考文档
- [ ] 贡献指南更新
- [ ] 架构设计文档

---

## 🚀 发布计划

### Beta 版本 (v1.0.0-beta.1)
- **时间**: 第 4 周
- **内容**: Phase 1 + Phase 2
- **目标**: 收集社区反馈

### RC 版本 (v1.0.0-rc.1)
- **时间**: 第 5 周
- **内容**: 所有功能 + 性能优化
- **目标**: 最终测试

### 正式版本 (v1.0.0)
- **时间**: 第 6 周
- **内容**: 完整功能 + 文档
- **发布**: GitHub Release + 官网

---

## ⚠️ 风险和挑战

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| egui 不支持原生标签页 | 高 | 使用 egui_dock 或自实现 |
| 插件系统复杂度高 | 高 | 先实现简单版本，逐步完善 |
| SOCKS5 实现困难 | 中 | 使用现有库 tokio-socks |
| 性能下降 | 中 | 持续性能监控和优化 |

---

## 📞 社区参与

### 征求反馈
- [ ] 创建 GitHub Discussion
- [ ] 发布功能投票
- [ ] 收集用户需求

### 贡献机会
- 插件开发
- 文档翻译
- 测试和 Bug 报告
- UI/UX 改进建议

---

## ✅ 里程碑检查清单

### Week 1-2: Phase 1
- [ ] 标签页管理器实现
- [ ] UI 集成
- [ ] 基本快捷键
- [ ] 单元测试

### Week 3: Phase 2
- [ ] 本地端口转发
- [ ] 远程端口转发
- [ ] SOCKS5 代理
- [ ] UI 和测试

### Week 4: Phase 3
- [ ] 跳板机支持
- [ ] UI 集成
- [ ] 测试
- [ ] **Beta 版本发布**

### Week 5: Phase 4
- [ ] 插件系统核心
- [ ] 3 个示例插件
- [ ] 插件市场 UI
- [ ] **RC 版本发布**

### Week 6: 最终发布
- [ ] 性能优化
- [ ] 文档完善
- [ ] 发布说明
- [ ] **v1.0.0 正式发布** 🎉

---

**文档版本**: 1.0  
**创建日期**: 2026-02-03  
**负责人**: iShell 开发团队

